# Com o mesmo programa de cálculos, adicione ao módulo uma função chamada resumo(), que mostre na tela algumas
# informações geradas pelas funções que já temos no módulo criado até aqui.

import Calculos


p = float(input('Digite o valor R$: '))
Calculos.resumo(p)
